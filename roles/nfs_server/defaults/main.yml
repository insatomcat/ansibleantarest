---
# Debian splits the server and the client tools in two packages; EL 9 ships
# both in nfs-utils.
nfs_server_packages: "{{ _nfs_server_packages_by_family[ansible_os_family] }}"
_nfs_server_packages_by_family:
  Debian:
    - nfs-kernel-server
    - nfs-common
  RedHat:
    - nfs-utils
